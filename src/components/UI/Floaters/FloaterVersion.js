import React from "react";
import "./FloaterVersion.css";

const FloaterVersion = (props) => {
    return (
        <div className="FloaterVersion">{props.children}</div>
    )
}

export default FloaterVersion;